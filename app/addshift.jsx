import { StyleSheet, Text, View } from "react-native";
import React from "react";

const addshift = () => {
  return (
    <View>
      <Text>Add a shift</Text> 
    </View>
  );
};

export default addshift;

const styles = StyleSheet.create({});
